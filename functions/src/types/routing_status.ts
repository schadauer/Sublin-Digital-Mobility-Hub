export enum RoutingStatus {
    initial = 'initial',
    active = 'active',
    unavailable = 'unavailable',
    canceled = 'canceled',
    timeOut = 'timeout',
    booked = 'booked',
    confirmed = 'confirmed',
}